Author(s): Braedon Larsen 
Created 6.11.26 
Updated: 
The pickeringInterface.py handles all interfacing to update the waveforms generated by the function generators. It consists of two functions the initPXIE() and updateWaveform(). 
NOTE: You MUST install the Pickering Direct IO drivers to your machine they are located in this repo under \PickeringManuals\Pickering_PXI_Driver_Package. This set of drivers handles the conversion from the C wrapper that is the python package to the IO interfacing. 

- initPXIE() - Run this function in the main loop, will locate and initilize the connection with the pickering cabinet and saves the bus locations and typing of the cards in the connection. As of 6.11.26 it is unknown how this script works with keeping the connection open
    - RETURNS - array of pickering card objects each object contains the bus and device that the card is located at. 

- updateWaveform() - updates the waveform of *card* on *channel* to be a sin waveform of frequency *frequency* (hz), amplitude *amplitude* (voltage ranging from 0), and offset *offset*
    - RETURNS - None

TODO: 
- Additional unit testing on phase offsetting 
- Testing of initPXIE methodology in other files 
- Connecting to Pickering device and maintaining connection 
- Disconnection behavior between funciton generator and code 
- Disconnection checks and reconnection behavior 
- More I haven't thought of - Braedon 